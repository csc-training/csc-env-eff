title: Using CSC HPC Environment Efficiently

plugins:
  - jekyll-sitemap

collections:
   hands-on:
        output: true

defaults:
  -
    scope:
      path: "_hands-on"
    values:
      layout: "default"
